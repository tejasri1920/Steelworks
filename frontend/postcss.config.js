// frontend/postcss.config.js
// PostCSS processes the CSS output from Tailwind.
// - tailwindcss: generates utility classes
// - autoprefixer: adds vendor prefixes for cross-browser compatibility
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
